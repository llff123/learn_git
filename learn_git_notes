1.初始化一个Git仓库，使用git init命令。
2.添加文件到Git仓库，分两步：
使用命令git add <file>，注意，可反复多次使用，添加多个文件；
使用命令git commit -m <message>，完成
3.查看git日志，使用git log指令；用git reflog查看命令历史
4.往上回滚一次 git reset --hard HEAD^(^代表上一个版本，～100代表上100个版本)

